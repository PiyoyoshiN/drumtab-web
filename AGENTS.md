# AGENTS.md

このリポジトリでは、DrumTab Web を「読み込んだ MIDI / JSON / 音声解析結果を見やすいドラム譜・ドラムタブ譜へ整えるアプリ」として育てます。

## 重要方針

- 高精度なドラム分離 AI や大規模な音声認識モデルは、このリポジトリの主目的ではありません。
- 外部ツールで得た MIDI / JSON / 解析結果を取り込み、譜面として読みやすく表示・再生・書き出しする体験を優先してください。
- 既存の読み込み、表示、再生、MIDI 書き出しを壊さないでください。
- 1コミット / 1PR では、できるだけ 1テーマに絞ってください。

## 優先順位

1. Grid 表示をよりドラム譜・ドラムタブらしくする。
2. 小節番号、拍、区切り線を見やすくする。
3. 8分 / 16分 / 32分表示切替を安定させる。
4. 休符や空白セルの見え方を改善する。
5. ハイハット、スネア、バスドラの基本譜面を読みやすくする。
6. Score 表示は本格化を急がず、まず壊れにくくする。
7. JSON サンプルと手動テストチェックリストを維持する。
8. 将来的な編集機能は設計を先に書き、小さく実装する。

## やらないこと

- ログイン
- サーバー保存
- 課金
- 外部 API 連携
- 高精度なドラム分離 AI
- 大規模な音声認識モデル
- いきなり完璧な五線譜化
- UI 全面作り直し

## 開発時の確認

- 変更後は原則 `npm run build` を実行してください。
- 表示に関わる変更では、可能ならサンプル JSON を使って Grid / Score / 再生 / MIDI 書き出しの手動確認を行ってください。
- TypeScript の型を保ち、try/catch で import を囲まないでください。

## ドキュメント方針

- 現在の機能と制約を README に反映してください。
- 譜面化強化のロードマップが変わる場合は README を更新してください。
- 手動確認項目が増える場合は `docs/manual-test-checklist.md` を更新してください。
- 新しい入力パターンを追加した場合は `public/samples/` に小さな JSON サンプルを追加してください。

## Production Sprint Mode

まとまった制作作業として依頼された場合は、以下を守ってください。

- 1回の制作作業では、小さな1修正だけで終了しないでください。
- 最初に目的、非目的、完了条件を確認してください。
- 作業を複数マイルストーンに分けて進めてください。
- 各マイルストーンで実装・確認・必要なら修正を行ってください。
- `npm run build` を実行し、結果を書いてください。
- build 未実行なら完了扱いにしないでください。
- 短時間で終わりそうな場合は、同じテーマ内の次マイルストーンへ進んでください。
- docs だけ、サンプルだけ、表記修正だけで終わらせないでください。
- ただし、無関係な機能で水増ししないでください。
- 実際に変更していない内容を PR 本文で「Added」「Implemented」と書かないでください。
- 残課題と次 Sprint 候補を書いてください。
